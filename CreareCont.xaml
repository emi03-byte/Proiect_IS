<Window x:Class="MultiTab.CreareCont"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Autentificare" Height="200" Width="300" WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Vertical" Grid.Row="0" Margin="0,0,0,10">
            <Label Content="Username:"/>
            <TextBox x:Name="UsernameTextBox" Width="200"/>
        </StackPanel>

        <StackPanel Orientation="Vertical" Grid.Row="1" Margin="0,0,0,10">
            <Label Content="Password:"/>
            <PasswordBox x:Name="PasswordBox" Width="200"/>
        </StackPanel>

        <Button Grid.Row="2" Content="Autentificare" Width="200" Click="LoginButton_Click"/>
    </Grid>
</Window>
